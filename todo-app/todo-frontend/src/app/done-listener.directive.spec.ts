import { DoneListenerDirective } from './done-listener.directive';

describe('DoneListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new DoneListenerDirective();
    expect(directive).toBeTruthy();
  });
});
