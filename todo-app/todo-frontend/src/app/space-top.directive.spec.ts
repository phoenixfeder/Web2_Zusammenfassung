import { SpaceTopDirective } from './space-top.directive';

describe('SpaceTopDirective', () => {
  it('should create an instance', () => {
    const directive = new SpaceTopDirective();
    expect(directive).toBeTruthy();
  });
});
